# PiCimOS WebAPI  
> auto generated by *PiCIMOS* at 2024.11.30-18.28.02  

## 目录  
模块数 16 总API数量 0106 总事件数量 0010  
### API清单  
#  
- [ModuleBase](#modulebase)  
- [Camera](#camera)  
	- [camera.focus 聚焦相机到对象](#camerafocus)  
	- [camera.fly 相机飞行](#camerafly)  
	- [camera.animControl 相机动画控制](#cameraanimcontrol)  
	- [camera.CameraSpeed 相机-缩放速度](#cameracameraspeed)  
- [Cloud](#cloud)  
	- [cloud.init 云渲染-云渲染初始化](#cloudinit)  
- [Environment](#environment)  
	- [environment.setTime 环境-设置时间](#environmentsettime)  
	- [environment.setWeather 环境-设置天气](#environmentsetweather)  
	- [environment.timepass 环境-时间流逝](#environmenttimepass)  
	- [environment.setPostProcessParam 环境-设置后处理参数](#environmentsetpostprocessparam)  
- [General](#general)  
	- [general.openUrl 通用-用系统默认浏览器打开网页地址](#generalopenurl)  
- [GIS](#gis)  
	- [gis.setPOIOptions GIS-设置POI选项](#gissetpoioptions)  
	- [gis.setDistrictOptions GIS-设置区域选项](#gissetdistrictoptions)  
	- [gis.loadVectorLayerFile GIS-加载矢量数据文件](#gisloadvectorlayerfile)  
	- [gis.loadVectorLayerData GIS-加载矢量图层数据](#gisloadvectorlayerdata)  
	- [gis.updatePresetLayer GIS-更新地形预设层](#gisupdatepresetlayer)  
	- [gis.updateVectorLayerFileById GIS-更新矢量图层文件](#gisupdatevectorlayerfilebyid)  
	- [gis.updateVectorLayerDataById GIS-更新矢量图层字符串](#gisupdatevectorlayerdatabyid)  
	- [gis.focusLayerById GIS-聚焦图层](#gisfocuslayerbyid)  
	- [gis.removeLayerById GIS-删除图层](#gisremovelayerbyid)  
	- [gis.setLayerVisibilityById GIS-设置图层可见性](#gissetlayervisibilitybyid)  
- [IOT](#iot)  
	- [iot.createObject IOT-创建交互对象](#iotcreateobject)  
	- [iot.objectStatusUpdate IOT-交互对象状态更新](#iotobjectstatusupdate)  
	- [iot.objectEventBinding IOT-交互对象事件绑定](#iotobjecteventbinding)  
	- [iot.objectPropertyChange IOT-交互对象属性变更](#iotobjectpropertychange)  
	- [iot.objectEvent IOT-对象事件](#iotobjectevent)  
	- [iot.objectAction IOT-交互对象动作](#iotobjectaction)  
- [Camera](#camera)  
	- [light.politlightlightintensity 点光源-光源强度](#lightpolitlightlightintensity)  
	- [light.pointlightlightradius 点光源-作用半径](#lightpointlightlightradius)  
	- [light.pointlightlightcolor 点光源-光源颜色](#lightpointlightlightcolor)  
	- [light.pointlightsourcelength 点光源-光源长度](#lightpointlightsourcelength)  
	- [light.pointlightshadowresolutionscale 点光源-阴影柔化](#lightpointlightshadowresolutionscale)  
	- [light.pointlightshadowsharpen 点光源-阴影锐化](#lightpointlightshadowsharpen)  
	- [light.spotlightlightintensity 聚光源-光源强度](#lightspotlightlightintensity)  
	- [light.spotlightlightcolor 聚光源-光源颜色](#lightspotlightlightcolor)  
	- [light.spotlightlightradius 聚光源-作用半径](#lightspotlightlightradius)  
	- [light.spotlightsourcelength 聚光源-光源长度](#lightspotlightsourcelength)  
	- [light.spotlightinnerconeangle 聚光源-光源内角](#lightspotlightinnerconeangle)  
	- [light.spotlightouterconeangle 聚光源-光源外角](#lightspotlightouterconeangle)  
	- [light.spotlightshadowresolutionscale 聚光源-阴影柔化](#lightspotlightshadowresolutionscale)  
	- [light.spotlightshadowsharpen 聚光源-阴影锐化](#lightspotlightshadowsharpen)  
	- [light.rectlightlightintensity 面光源-光源强度](#lightrectlightlightintensity)  
	- [light.rectlightlightradius 面光源-作用半径](#lightrectlightlightradius)  
	- [light.rectlightlightcolor 面光源-光源颜色](#lightrectlightlightcolor)  
	- [light.rectlightsourcewidth 面光源-光源宽度](#lightrectlightsourcewidth)  
	- [light.rectlightsourceheight 面光源-光源高度](#lightrectlightsourceheight)  
	- [light.rectlightbarndoorangle 面光源-矩边角度](#lightrectlightbarndoorangle)  
	- [light.rectlightbarndoorlength 面光源-矩边长度](#lightrectlightbarndoorlength)  
	- [light.directlightlightintensity 定向光源-光源强度](#lightdirectlightlightintensity)  
	- [light.directlightlightangle 定向光源-光源角度](#lightdirectlightlightangle)  
	- [light.directlightlightcolor 定向光源-光源颜色](#lightdirectlightlightcolor)  
	- [light.directlightshadowdistance 定向光源-阴影距离](#lightdirectlightshadowdistance)  
- [Mode](#mode)  
	- [mode.enterEditMode 模式-进入编辑模式](#modeentereditmode)  
	- [mode.enterPlayMode 模式-进入预览模式](#modeenterplaymode)  
	- [mode.event 模式-通用事件](#modeevent)  
	- [mode.enableTransparency 模式-启用设计器UI传统](#modeenabletransparency)  
	- [scene.moveCameraByOffset 场景-相机平面偏移相机](#scenemovecamerabyoffset)  
- [Objects](#objects)  
	- [object.create 对象-创建](#objectcreate)  
	- [object.move 对象-移动](#objectmove)  
	- [object.visible 对象-显示隐藏](#objectvisible)  
	- [object.destroy 对象-销毁](#objectdestroy)   
	- [object.layerVisible 对象-图层显隐](#objectlayervisible)  
	- [object.setMaterial 对象-设置材质](#objectsetmaterial)  
	- [object.setProperty 对象-设置属性](#objectsetproperty)  
	- [object.callFunction 对象-调用函数](#objectcallfunction)  
	- [object.playAnimation 对象-播放动画](#objectplayanimation)  
	- [object.playAnimationSet 对象-播放动画集](#objectplayanimationset)  
	- [object.playScatterSetAnimation 对象-播放阵列动画](#objectplayscattersetanimation)  
	- [object.resetScatterSetWithId 对象-重置阵列对象](#objectresetscattersetwithid)  
	- [object.setWaterMaterialProperty 对象-修改水材质属性](#objectsetwatermaterialproperty)  
	- [object.blur 对象-虚化](#objectblur)  
	- [Object.Opacity 对象-透明度](#objectopacity)  
	- [object.GetInfo 对象-信息](#objectgetinfo)  
	- [object.GetScreenInfo 对象-屏幕信息](#objectgetscreeninfo)  
	- [object.StopScreenInfo 对象-停止屏幕信息](#objectstopscreeninfo)  
- [Level](#level)  
	- [pathfollow.getchecklist 巡检-获取巡检目标](#pathfollowgetchecklist)  
	- [pathfollow.controll 巡检-控制](#pathfollowcontroll)  
	- [camera.zoom 相机-缩放](#camerazoom)  
	- [updatelocation.withreference 更新位置-以参照物为基点](#updatelocationwithreference)  
- [POI](#poi)  
	- [poi.createPOI POI-创建POI](#poicreatepoi)  
	- [poi.modifyPOI POI-修改POI](#poimodifypoi)  
	- [poi.loadPOIFromFile POI-从文件批量加载POI](#poiloadpoifromfile)  
	- [poi.loadPOIFromData POI-从数据批量加载POI](#poiloadpoifromdata)  
	- [poi.removePOIFromGroup POI-从POI组移除POI](#poiremovepoifromgroup)  
- [Project](#project)  
	- [project.new 项目-新建项目](#projectnew)  
	- [project.load 项目-加载项目](#projectload)  
	- [project.save 项目-保存项目](#projectsave)  
- [UI](#ui)  
- [Water](#water)  
	- [water.loadMesh 水利-加载水体网格结构](#waterloadmesh)  
	- [water.loadMeshFromData 水利-从数据加载水体网格结构](#waterloadmeshfromdata)  
	- [water.focusOnTile 水利-聚焦到网格](#waterfocusontile)  
	- [water.loadStatus 水利-加载水体状态数据](#waterloadstatus)  
	- [water.playConfig 水利-播放状态的控制](#waterplayconfig)  
	- [water.getMeshStat 水利-水体网格的统计](#watergetmeshstat)  
	- [water.getStatusStat 水利-状态数据的统计](#watergetstatusstat)  
	- [water.getStatusAtPos 水利-获取指定位置状态](#watergetstatusatpos)  
	- [water.setStatusAtPos 水利-更新指定位置状态](#watersetstatusatpos)  
	- [water.setStatusConfig 水利-设置水体状态的配置](#watersetstatusconfig)  
	- [water.setDrawCount 水利-设置绘制数量](#watersetdrawcount)  
- [Window](#window)  
	- [window.close 窗口-关闭](#windowclose)  
	- [window.move 窗口-移动位置](#windowmove)  
	- [window.resize 窗口-更改大小](#windowresize)  
	- [window.createByCamera 窗口-创建相机窗口](#windowcreatebycamera)  
	- [window.createByWidget 窗口-创建原生界面窗口](#windowcreatebywidget)  
	- [window.createByWebPage 窗口-创建网页窗口](#windowcreatebywebpage)  
### 事件清单  
#  

- [ModuleBase](#modulebase)  
- [Camera](#camera)  
- [Cloud](#cloud)  
- [Environment](#environment)  
- [General](#general)  
- [GIS](#gis)  
- [IOT](#iot)  
- [Camera](#camera)  
- [Mode](#mode)  
- [Objects](#objects)  
	- [object.onClick 对象-被点击](#objectonclick)  
	- [object.onDoubleClick 对象-被双击](#objectondoubleclick)  
	- [object.onMoved 对象-被移动](#objectonmoved)  
	- [object.onScale 对象-缩放](#objectonscale)  
	- [object.onVisible 对象-显隐](#objectonvisible)  
	- [object.onCreate 对象-创建](#objectoncreate)  
	- [object.onDestroy 对象-销毁](#objectondestroy)  
	- [object.onAttach 对象-附加](#objectonattach)  
	- [object.onDetach 对象-脱离](#objectondetach)  
- [Level](#level)  
	- [pathfollow.onCheck 巡检-检查对象](#pathfollowoncheck)  
- [POI](#poi)  
- [Project](#project)  
- [UI](#ui)  
- [Water](#water)  
- [Window](#window)  
# API详情  
  

## ModuleBase   
#  
  
[回目录](#目录)  API数量 0000  

## Camera   
#  
  
[回目录](#目录)  API数量 0004  
### camera.focus  
聚焦相机到对象  [回目录](#目录)  
聚焦相机到对象  
  
#### 输入  
 
```javascript  
{
	target: "", //target object name
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript  
piclient.emit("camera.focus",{
	target: "", //target object name
})
```  
  
### camera.fly  
相机飞行  [回目录](#目录)  
相机飞行  
  
#### 输入  
 
```javascript  
{
	target: "", //target object name
	ecef: "1234,5678,4321", //ecef坐标
	geo: "123.1234,31.1234,20", //经纬度
	rotation: "0,-30,45.23", //旋转 roll, pitch, yaw的角度值
	time: 3.12 //飞行时长，单位秒
	//target,ecef,geo 三选一，同时提供则选用的优先级为 target >> geo >> ecef
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("camera.fly",{
	target: "", //target object name
	ecef: "1234,5678,4321", //ecef坐标
	geo: "123.1234,31.1234,20", //经纬度
	rotation: "0,-30,45.23", //旋转 roll, pitch, yaw的角度值
	time: 3.12 //飞行时长，单位秒
	//target,ecef,geo 三选一，同时提供则选用的优先级为 target >> geo >> ecef
})  
```  
  
### camera.animControl  
相机动画控制  [回目录](#目录)  
相机动画控制，设置动画时长，播放，暂停，停止  
  
#### 输入  
 

```javascript  
{
	target: "",
	totalTime: 3.2,
	playState: "", //stop,pause,play,toggle
	bFromStart: false
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("camera.animControl",{
	target: "",
	totalTime: 3.2,
	playState: "", //stop,pause,play,toggle
	bFromStart: false
})  
```  
  
### camera.CameraSpeed  
相机-缩放速度  [回目录](#目录)  
相机缩放速度  
  
#### 输入  
 
```javascript  
{

	CameraSpeed: 4.0 //相机速率

}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript  
```  
  

## Cloud   
#  
云渲染相关的接口  
[回目录](#目录)  API数量 0001  
### cloud.init  
云渲染-云渲染初始化  [回目录](#目录)  
云渲染初始化  
  
#### 输入  
 
```typescript
{
	accountId: "", //账号ID

	accountName:"", //账号名
	token: "", //token
	projectId: "", //打开的项目ID
	showUI: false, //是否显示UI
	editorMode: false //是否进入编辑模式
}
```  
  
#### 输出  
 
```typescript  
{
	success: true, 
	msg: "",
}
```  
  
#### 调用  
 
```typescript
piclient.emit("cloud.init",{
	accountId: "", //账号ID

	accountName:"", //账号名
	token: "", //token
	projectId: "", //打开的项目ID
	showUI: false, //是否显示UI
	editorMode: false //是否进入编辑模式
})  
```  
  

## Environment   
#  
模式模块主要做环境的管理  
[回目录](#目录)  API数量 0004  
### environment.setTime  
环境-设置时间  [回目录](#目录)  
设置时间  
  
#### 输入  
 
```javascript  
{
	hour:15, //小时, 0-23
	minute: 30, //分钟 0-59
	percentOfDay: 0 //一天的百分比 0-1，大于0时hour/minute无效
}  
```  
  
#### 输出  
 
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("environment.setTime",{
	hour:15, //小时, 0-23
	minute: 30, //分钟 0-59
	percentOfDay: 0 //一天的百分比 0-1，大于0时hour/minute无效
})  
```  
  
### environment.setWeather  
环境-设置天气  [回目录](#目录)  
设置天气  
  
#### 输入  
  
```javascript  
{
	weather: 2 //天气, 1-11 晴空万里/少云/多云/阴天/雾天/小雨/中雨/暴风雨/小雪/中雪/暴风雪
}  
```  
  
#### 输出  
  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("environment.setWeather",{
	weather: 2 //天气, 1-11 晴空万里/少云/多云/阴天/雾天/小雨/中雨/暴风雨/小雪/中雪/暴风雪
})  
```  
  
### environment.timepass  
环境-时间流逝  [回目录](#目录)  
时光流逝控制  
  
#### 输入  
 
```javascript  
{
	date: "2022-08-27",
	time: "14:40",
	timeSpeed: 1.0
}  
```  
  
#### 输出  
 
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("environment.timepass",{
	date: "2022-08-27",
	time: "14:40",
	timeSpeed: 1.0
})  
```  
  
### environment.setPostProcessParam  
环境-设置后处理参数  [回目录](#目录)  
设置后处理参数  
  
#### 输入  
 
```javascript  
{
	ppv: "", //后处理盒子名称，为空表示选用默认的
	paramName: "", //属性名
	paramValue: "" //属性值
	paramType: "" //属性类型
}  
```  
  
#### 输出  
 
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("environment.setPostProcessParam",{
	ppv: "", //后处理盒子名称，为空表示选用默认的
	paramName: "", //属性名
	paramValue: "" //属性值
	paramType: "" //属性类型
})  
```  
  

## General   
#  
通用模块，提供通用功能与服务  
[回目录](#目录)  API数量 0002  
### general.openUrl  
通用-用系统默认浏览器打开网页地址  [回目录](#目录)  
打开URL  
  
#### 输入  
 
```typescript
interface OpenURLMsg {
	url: string,
}
```  
示例数据  
```javascript  
{
	url: "" //地址
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("general.openUrl",{
	url: "" //地址
})  
```  
  

## GIS   
#  
GIS模块，提供地理消息行业支持  
[回目录](#目录)  API数量 0010  
### gis.setPOIOptions  
GIS-设置POI选项  [回目录](#目录)  
设置地理相关的POI的显示选项  
  
#### 输入  
 
```typescript  
{
	enable: true, //是否启用显示，false则不显示
	radius: 300,  //显示半径多少米，相机为中心点方圆多少米内的poi点位 
	level: 10,    //GIS比例尺级别，1-18, 0=自动
	queryType: "1", //查询类型， 1（普通POI），2（统计），3（行政区)，4（建议词搜索），5（线路结果） http://lbs.tianditu.gov.cn/server/search2.html
	dataType: "行政地名,加油站,水利设施", //显示的类型，详情参考 https://download.tianditu.gov.cn/download/xzqh/Type.csv
	maxNum: 1000, //最大显示数量
	color: "ff0000", //显示颜色
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.setPOIOptions",{
	enable: true, //是否启用显示，false则不显示
	radius: 300,  //显示半径多少米，相机为中心点方圆多少米内的poi点位 
	level: 10,    //GIS比例尺级别，1-18, 0=自动
	queryType: "1", //查询类型， 1（普通POI），2（统计），3（行政区)，4（建议词搜索），5（线路结果） http://lbs.tianditu.gov.cn/server/search2.html
	dataType: "行政地名,加油站,水利设施", //显示的类型，详情参考 https://download.tianditu.gov.cn/download/xzqh/Type.csv
	maxNum: 1000, //最大显示数量
	color: "ff0000", //显示颜色
})  
```  
  
### gis.setDistrictOptions  
GIS-设置区域选项  [回目录](#目录)  
设置行政区域，项目区域等区域的显示选项  
  
#### 输入  
 
```typescript  
{
	enable: true, //是否启用显示，false则不显示
	borderThickness: 300,  //边缘粗细，单位米
	borderColor: "ff00ff7f", //边缘颜色，支持半透明RGBA
	borderHeight: 0.3, //边缘厚度，单位米
	fillColor: "2f00ff3f", //填充颜色，支持半透明RGBA
	fillHeight: 0.1, //填充区域厚度，单位米
	textVisible: false, //区域名称是否显示
	textColor: "000000", //文本颜色
	textScale: 1.0, //文本大小缩放
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.setDistrictOptions",{
	enable: true, //是否启用显示，false则不显示
	borderThickness: 300,  //边缘粗细，单位米
	borderColor: "ff00ff7f", //边缘颜色，支持半透明RGBA
	borderHeight: 0.3, //边缘厚度，单位米
	fillColor: "2f00ff3f", //填充颜色，支持半透明RGBA
	fillHeight: 0.1, //填充区域厚度，单位米
	textVisible: false, //区域名称是否显示
	textColor: "000000", //文本颜色
	textScale: 1.0, //文本大小缩放
})  
```  
  
### gis.loadVectorLayerFile  
GIS-加载矢量数据文件  [回目录](#目录)  
全球环境下加载矢量数据文件  
  
#### 输入  
 
```typescript
{
	filePath : "", //文件路径
	offset : "0.0, 0.0, 0.0", // 缺省
	layerType : 0, // 图层类型

	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff", // 填充颜色
	faceMaterialId : "materialId" // 面材质Id
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.loadVectorLayerFile",{
	filePath : "", //文件路径
	offset : "0.0, 0.0, 0.0", // 缺省
	layerType : 0, // 图层类型

	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff", // 填充颜色
	faceMaterialId : "materialId" // 面材质Id
})  
```  
  
### gis.loadVectorLayerData  
GIS-加载矢量图层数据  [回目录](#目录)  
全球环境下加载矢量图层数据  
  
#### 输入  
 
```typescript
{
	// 通过数据创建图层只支持 GeoJson和CustomJson
	data : "", // 图层数据
	offset : "0.0, 0.0, 0.0", // 缺省
	layerType : 1, // 图层类型，1 GeoJson，2 CustomJson

	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff", // 填充颜色
	faceMaterialId : "materialId" // 面材质Id
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.loadVectorLayerData",{
	// 通过数据创建图层只支持 GeoJson和CustomJson
	data : "", // 图层数据
	offset : "0.0, 0.0, 0.0", // 缺省
	layerType : 1, // 图层类型，1 GeoJson，2 CustomJson

	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff", // 填充颜色
	faceMaterialId : "materialId" // 面材质Id
})  
```  
  
### gis.updatePresetLayer  
GIS-更新地形预设层  [回目录](#目录)  
全球环境下更新地形预设层  
  
#### 输入  
 
```typescript
{
	markLayerType : 0, // 0 不显示标记图层，1 仅显示地名标记，2 仅显示边界，3 仅显示地名和路网
	enableLandHeight : true, // true 开启地形高度，false不开启地形高度
	backgroundLayerType : 0 // 0 必应影像，1 天地图地形，2 天地图矢量，3 天地图影像
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.updatePresetLayer",{
	markLayerType : 0, // 0 不显示标记图层，1 仅显示地名标记，2 仅显示边界，3 仅显示地名和路网
	enableLandHeight : true, // true 开启地形高度，false不开启地形高度
	backgroundLayerType : 0 // 0 必应影像，1 天地图地形，2 天地图矢量，3 天地图影像
})  
```  
  
### gis.updateVectorLayerFileById  
GIS-更新矢量图层文件  [回目录](#目录)  
全球环境下更新矢量图层文件  
  
#### 输入  
 
```typescript
{
	layerId : "", // 对象ID
	layerType : 0, // 图层类型
	filePath : "D:/Sample.json", // 文件路径

	offset : "0.0, 0.0, 0.0", // 缺省
	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff" // 填充颜色
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.updateVectorLayerFileById",{
	layerId : "", // 对象ID
	layerType : 0, // 图层类型
	filePath : "D:/Sample.json", // 文件路径

	offset : "0.0, 0.0, 0.0", // 缺省
	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff" // 填充颜色
})  
```  
  
### gis.updateVectorLayerDataById  
GIS-更新矢量图层字符串  [回目录](#目录)  
全球环境下更新矢量图层字符串  
  
#### 输入  
 
```typescript
{
	layerId : "", // 对象ID
	layerType : 1, // 图层类型，1 GeoJson，2 CustomJson
	data : "", // 数据结构与图层类型相关

	offset : "0.0, 0.0, 0.0", // 缺省
	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff" // 填充颜色
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.updateVectorLayerDataById",{
	layerId : "", // 对象ID
	layerType : 1, // 图层类型，1 GeoJson，2 CustomJson
	data : "", // 数据结构与图层类型相关

	offset : "0.0, 0.0, 0.0", // 缺省
	enablePoint : false, // 是否显示顶点
	enableLine : true, // 是否显示线
	enableFace : false, // 是否显示面
	snapLand : false, // 是否吸附地形
	pointColor : "#ffffff", // 顶点颜色
	lineColor : "#ffffff", // 轮廓颜色
	faceColor : "#ffffff" // 填充颜色
})  
```  
  
### gis.focusLayerById  
GIS-聚焦图层  [回目录](#目录)  
全球环境下聚焦到特定图层  
  
#### 输入  
 
```typescript
{
	layerId : "", // 对象ID
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.focusLayerById",{
	layerId : "", // 对象ID
}) 
```  
  
### gis.removeLayerById  
GIS-删除图层  [回目录](#目录)  
全球环境下删除特定图层  
  
#### 输入  
 
```typescript
{
	layerId : "", // 对象ID
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.removeLayerById",{
	layerId : "", // 对象ID
})  
```  
  
### gis.setLayerVisibilityById  
GIS-设置图层可见性  [回目录](#目录)  
全球环境下设置图层可见性  
  
#### 输入  
 
```typescript
{
	layerId : "", // 对象ID
	visiable : true, // 是否可见
}
```
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("gis.setLayerVisibilityById",{
	layerId : "", // 对象ID
	visiable : true, // 是否可见
})  
```  
  

## IOT   
#  
IOT模块，提供物联网行业支持  
[回目录](#目录)  API数量 0006  
### iot.createObject  
IOT-创建交互对象  [回目录](#目录)  
创建物联网交互对象  
  
#### 输入  
 
```typescript  
{
	name: "objectName", //场景中3d对象名
	type: "", //类型
	geoPos: "121.1234,31.1234,0", //经纬度
	parent: "", //附加的父对象名，为空则独立
	relativePos: "x,y,z", //相对位置
	rotation: "roll,pitch,yaw", //朝向    
	iotId: "", //iot系统的设备ID
} 
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("iot.createObject",{
	name: "objectName", //场景中3d对象名
	type: "", //类型
	geoPos: "121.1234,31.1234,0", //经纬度
	parent: "", //附加的父对象名，为空则独立
	relativePos: "x,y,z", //相对位置
	rotation: "roll,pitch,yaw", //朝向    
	iotId: "", //iot系统的设备ID
})  
```  
  
### iot.objectStatusUpdate  
IOT-交互对象状态更新  [回目录](#目录)  
交互对象的状态更新  
  
#### 输入  
 
```typescript  
{
	url: "" //地址
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("iot.objectStatusUpdate",{
	url: "" //地址
})  
```  
  
### iot.objectEventBinding  
IOT-交互对象事件绑定  [回目录](#目录)  
交互对象事件绑定  
  
#### 输入  
 
```typescript  
{
	url: "" //地址
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("iot.objectEventBinding",{
	url: "" //地址
})  
```  
  
### iot.objectPropertyChange  
IOT-交互对象属性变更  [回目录](#目录)  
交互对象属性变更  
  
#### 输入  
 
```typescript  
{
	iotId: "", //目标设备
	propName: "", //属性名
	propValue: "",//属性值
} 
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("iot.objectPropertyChange",{
	iotId: "", //目标设备
	propName: "", //属性名
	propValue: "",//属性值
})  
```  
  
### iot.objectEvent  
IOT-对象事件  [回目录](#目录)  
交互对象事件绑定  
  
#### 输入  
 
```typescript  
{
	iotId: "", //目标设备
	eventName: "", //事件名
	props: [ //参数列表
	{name: "", value: ""}, {}
]
}
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("iot.objectEvent",{
	iotId: "", //目标设备
	eventName: "", //事件名
	props: [ //参数列表
	{name: "", value: ""}, {}
]
})  
```  
  
### iot.objectAction  
IOT-交互对象动作  [回目录](#目录)  
交互对象动作触发  
  
#### 输入  
 
```typescript  
{
	iotId: "", //目标设备
	actionName: "", //动作名
	props: [ //参数列表
	{name: "", value: ""}, {}
]
}
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("iot.objectAction",{
	iotId: "", //目标设备
	actionName: "", //动作名
	props: [ //参数列表
	{name: "", value: ""}, {}
]
})  
```  
  

## Camera   
#  
  
[回目录](#目录)  API数量 0025  
### light.politlightlightintensity  
点光源-光源强度  [回目录](#目录)  
点光源-光源强度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightintensity: 1 //光源强度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.politlightlightintensity",{
	target: "", //目标
	lightintensity: 1 //光源强度
})  
```  
  
### light.pointlightlightradius  
点光源-作用半径  [回目录](#目录)  
点光源-作用半径  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightradius: 40000 //作用半径
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.pointlightlightradius",{
	target: "", //目标
	lightradius: 40000 //作用半径
})  
```  
  
### light.pointlightlightcolor  
点光源-光源颜色  [回目录](#目录)  
点光源-光源颜色  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.pointlightlightcolor",{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
})  
```  
  
### light.pointlightsourcelength  
点光源-光源长度  [回目录](#目录)  
点光源-光源长度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	sourcelength: 0 //光源长度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.pointlightsourcelength",{
	target: "", //目标
	sourcelength: 0 //光源长度
})  
```  
  
### light.pointlightshadowresolutionscale  
点光源-阴影柔化  [回目录](#目录)  
点光源-阴影柔化  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	shadowresolutionscale: 0 //阴影柔化
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.pointlightshadowresolutionscale",{
	target: "", //目标
	shadowresolutionscale: 0 //阴影柔化
})  
```  
  
### light.pointlightshadowsharpen  
点光源-阴影锐化  [回目录](#目录)  
点光源-阴影锐化  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	shadowsharpen: 0 //阴影锐化
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.pointlightshadowsharpen",{
	target: "", //目标
	shadowsharpen: 0 //阴影锐化
})  
```  
  
### light.spotlightlightintensity  
聚光源-光源强度  [回目录](#目录)  
聚光源-光源强度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightintensity: 1 //光源强度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightlightintensity",{
	target: "", //目标
	lightintensity: 1 //光源强度
})  
```  
  
### light.spotlightlightcolor  
聚光源-光源颜色  [回目录](#目录)  
聚光源-光源颜色  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightlightcolor",{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
})  
```  
  
### light.spotlightlightradius  
聚光源-作用半径  [回目录](#目录)  
聚光源-作用半径  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightradius: 40000 //作用半径
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightlightradius",{
	target: "", //目标
	lightradius: 40000 //作用半径
})  
```  
  
### light.spotlightsourcelength  
聚光源-光源长度  [回目录](#目录)  
聚光源-光源长度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	sourcelength: 0 //光源长度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightsourcelength",{
	target: "", //目标
	sourcelength: 0 //光源长度
})  
```  
  
### light.spotlightinnerconeangle  
聚光源-光源内角  [回目录](#目录)  
聚光源-光源内角  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	innerconeangle: 0 //光源内角
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightinnerconeangle",{
	target: "", //目标
	innerconeangle: 0 //光源内角
})  
```  
  
### light.spotlightouterconeangle  
聚光源-光源外角  [回目录](#目录)  
聚光源-光源外角  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	outerconeangle: 0 //光源内角
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightouterconeangle",{
	target: "", //目标
	outerconeangle: 0 //光源内角
})  
```  
  
### light.spotlightshadowresolutionscale  
聚光源-阴影柔化  [回目录](#目录)  
聚光源-阴影柔化  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	shadowresolutionscale: 0 //阴影柔化
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightshadowresolutionscale",{
	target: "", //目标
	shadowresolutionscale: 0 //阴影柔化
})  
```  
  
### light.spotlightshadowsharpen  
聚光源-阴影锐化  [回目录](#目录)  
聚光源-阴影锐化  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	shadowsharpen: 0 //阴影锐化
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.spotlightshadowsharpen",{
	target: "", //目标
	shadowsharpen: 0 //阴影锐化
})  
```  
  
### light.rectlightlightintensity  
面光源-光源强度  [回目录](#目录)  
面光源-光源强度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightintensity: 1 //光源强度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightlightintensity",{
	target: "", //目标
	lightintensity: 1 //光源强度
})  
```  
  
### light.rectlightlightradius  
面光源-作用半径  [回目录](#目录)  
面光源-作用半径  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightradius: 40000 //作用半径
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightlightradius",{
	target: "", //目标
	lightradius: 40000 //作用半径
})  
```  
  
### light.rectlightlightcolor  
面光源-光源颜色  [回目录](#目录)  
面光源-光源颜色  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightlightcolor",{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
})  
```  
  
### light.rectlightsourcewidth  
面光源-光源宽度  [回目录](#目录)  
面光源-光源宽度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	sourcewidth: 1 //光源宽度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightsourcewidth",{
	target: "", //目标
	sourcewidth: 1 //光源宽度
})  
```  
  
### light.rectlightsourceheight  
面光源-光源高度  [回目录](#目录)  
面光源-光源高度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	sourceheight: 1 //光源高度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightsourceheight",{
	target: "", //目标
	sourceheight: 1 //光源高度
})  
```  
  
### light.rectlightbarndoorangle  
面光源-矩边角度  [回目录](#目录)  
面光源-矩边角度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	barndoorangle: 0 //矩边角度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightbarndoorangle",{
	target: "", //目标
	barndoorangle: 0 //矩边角度
})  
```  
  
### light.rectlightbarndoorlength  
面光源-矩边长度  [回目录](#目录)  
面光源-矩边长度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	barndoorlength: 0 //矩边角度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.rectlightbarndoorlength",{
	target: "", //目标
	barndoorlength: 0 //矩边角度
})  
```  
  
### light.directlightlightintensity  
定向光源-光源强度  [回目录](#目录)  
定向光源-光源强度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightintensity: 1.0 //光源强度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.directlightlightintensity",{
	target: "", //目标
	lightintensity: 1.0 //光源强度
})  
```  
  
### light.directlightlightangle  
定向光源-光源角度  [回目录](#目录)  
定向光源-光源角度  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightrotation: 0.0, 0.0, 0.0 //光源角度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.directlightlightangle",{
	target: "", //目标
	lightrotation: 0.0, 0.0, 0.0 //光源角度
})  
```  
  
### light.directlightlightcolor  
定向光源-光源颜色  [回目录](#目录)  
定向光源-光源颜色  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.directlightlightcolor",{
	target: "", //目标
	lightcolor: 0.0, 0.0, 0.0 //光源颜色
})  
```  
  
### light.directlightshadowdistance  
定向光源-阴影距离  [回目录](#目录)  
定向光源-阴影距离  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	shadowdistance: 40000 //阴影距离
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("light.directlightshadowdistance",{
	target: "", //目标
	shadowdistance: 40000 //阴影距离
})  
```  
  

## Mode   
#  
模式模块主要做编辑模式的管理  
[回目录](#目录)  API数量 0005  
### mode.enterEditMode  
模式-进入编辑模式  [回目录](#目录)  
进入编辑模式  
  
#### 输入  
 
	```javascript  
	{  
		"modeId": 2,  //模式，0=摆放，1=画刷，2=路线...
		"mainPage": -1 //主页面序号 0-7,  -1=不修改
		"subPage": -1 //子页面序号 ,-1=不修改
	}  
	```  
	  
#### 输出  
 
	```javascript  
	{  
	}  
	```
	  
#### 调用  
 
```typescript
piclient.emit("mode.enterEditMode",	{  
		"modeId": 2,  //模式，0=摆放，1=画刷，2=路线...
		"mainPage": -1 //主页面序号 0-7,  -1=不修改
		"subPage": -1 //子页面序号 ,-1=不修改
	})  
```  
  
### mode.enterPlayMode  
模式-进入预览模式  [回目录](#目录)  
进入预览模式  
  
#### 输入  
 
	```javascript  
	{  
		"defaultPageId": "" //默认加载的页面ID
	}  
	```  
	  
#### 输出  
 
	```javascript  
	{  
	}  
	```
	  
#### 调用  
 
```typescript
piclient.emit("mode.enterPlayMode",{  
		"defaultPageId": "" //默认加载的页面ID
	})  
```  
  
### mode.event  
模式-通用事件  [回目录](#目录)  
通用的事件触发，从外部通知PiClient事件的发生  
  
#### 输入  
 
	```javascript  
	{  
		"action": "page.loadCompleted",
		"target": "page",
		"params": { param1: "value1", param2: "0" }
	}  
	```  
	  
#### 输出  
 
	```javascript  
	{  
	}  
	```
	  
#### 调用  
 
```typescript
  piclient.emit("mode.event",{  
		"action": "page.loadCompleted",
		"target": "page",
		"params": { param1: "value1", param2: "0" }
	})
```  
  
### mode.enableTransparency  
模式-启用设计器UI传统  [回目录](#目录)  
是否让设计器的UI可以穿透交互  
  
#### 输入  
 
	```javascript  
	{  
		"action": "mode.enableTransparency",
		"params": { enable: true }
	}  
	```  
	  
#### 输出  
 
	```javascript  
	{  
	}  
	```
	  
#### 调用  
 
```typescript
piclient.emit("mode.enableTransparency",{  
		"action": "mode.enableTransparency",
		"params": { enable: true }
	})  
```  
  
### scene.moveCameraByOffset  
场景-相机平面偏移相机  [回目录](#目录)  
在相机平面偏移相机  
  
#### 输入  
 
	```javascript  
	{  
		"action": "scene.moveCameraByOffset",
		"params": { offsetX: 0, offsetY: 0 }
	}  
	```  
	  
#### 输出  
 
	```javascript  
	{  
	}  
	```
	  
#### 调用  
 
```typescript
piclient.emit("scene.moveCameraByOffset",{  
		"action": "scene.moveCameraByOffset",
		"params": { offsetX: 0, offsetY: 0 }
	})  
```  
  

## Objects   
#  
  
[回目录](#目录)  API数量 0019  
### object.create  
对象-创建  [回目录](#目录)  
创建对象  
  
#### 输入  
 
```javascript  
{  
	objectId: "", //对象Id
	ecef: "", //ecef坐标
	geo: "", //经纬度坐标
	location: "", // 笛卡尔坐标 x,y,z
	rotation: "", //朝向 roll,pitch,yaw
	scale: "", // 缩放 x,y,z
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.create",{  
	objectId: "", //对象Id,选择模型后点击模型栏右上角的切换预览面板显示状态按钮，预览面板的对象ID的值
	ecef: "", //ecef坐标
	geo: "", //经纬度坐标
	location: "", // 笛卡尔坐标 x,y,z
	rotation: "", //朝向 roll,pitch,yaw
	scale: "", // 缩放 x,y,z
})  
```  
  
### object.move  
对象-移动  [回目录](#目录)  
移动对象，参数offset/ecef/geo三选一，都提供则geo >> ecef >> offset  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
	parent: "", //父对象
	offset: "", //偏移 x,y,z
	ecef: "", //ecef坐标
	geo: "", //经纬度
	rotation: "" //朝向 roll,pitch,yaw
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.move",{
	target: "", //目标
	parent: "", //父对象
	offset: "", //偏移 x,y,z
	ecef: "", //ecef坐标
	geo: "", //经纬度
	rotation: "" //朝向 roll,pitch,yaw
})  
```  
  
### object.visible  
对象-显示隐藏  [回目录](#目录)  
显示隐藏对象  
  
#### 输入  
  
```javascript  
{
	target: "", //单个目标
	targets: ["",""],//多个目标，单个目标和多个目标互斥，使用哪个另一个就需要删掉
	visible: "", //可见性 show / hide / toggle
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("object.visible",{
	target: "", //单个目标
	targets: ["",""],//多个目标，单个目标和多个目标互斥，使用哪个另一个就需要删掉
	visible: "", //可见性 show / hide / toggle
})  
```  
  
### object.destroy  
对象-销毁  [回目录](#目录)  
销毁对象  
  
#### 输入  
 
```javascript  
{
	target: "", //目标
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("object.destroy",{
	target: "", //目标
})  
```  
  
### object.layerVisible  
对象-图层显隐  [回目录](#目录)  
图层显隐  
  
#### 输入  
 
```javascript  
{
	layerName: "", //图层名
	visible: "", //可见性 show / hide / toggle
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.layerVisible",{
	layerName: "", //图层名
	visible: "", //可见性 show / hide / toggle
})  
```  
  
### object.setMaterial  
对象-设置材质  [回目录](#目录)  
设置材质  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	componentIndex: 0,//组件序号, -1表示所有组件全部替换
	sectionIndex: 0, //材质位置序号，-1表示所有部位全部替换
	materialId: "", //材质ID
	children: false, //递归子级，是否连所有子对象一起处理
	matParams: { //材质的参数微调，可选
	 paramName01: paramValue01,
	 paramName02: paramValue02
   }
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.setMaterial",{
	target: "", //对象名称
	componentIndex: 0,//组件序号, -1表示所有组件全部替换
	sectionIndex: 0, //材质位置序号，-1表示所有部位全部替换
	materialId: "", //材质ID
	children: false, //递归子级，是否连所有子对象一起处理
	matParams: { //材质的参数微调，可选
	 paramName01: paramValue01,
	 paramName02: paramValue02
   }
})  
```  
  
### object.setProperty  
对象-设置属性  [回目录](#目录)  
修改对象属性，仅适用于修改值后能及时生效的属性  
  
#### 输入  
 
```javascript  
{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	propName: "", //属性名
	propType: "", //属性类型
	propValue: "", //属性值
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("object.setProperty",{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	propName: "", //属性名
	propType: "", //属性类型
	propValue: "", //属性值
})  
```  
  
### object.callFunction  
对象-调用函数  [回目录](#目录)  
调用函数，暂不支持传参，可以是Actor子类的蓝图目标  
  
#### 输入  
 
```javascript  
{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	function: "", //函数名, 暂不支持传参
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("object.callFunction",{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	function: "", //函数名, 暂不支持传参
})  
```  
  
### object.playAnimation  
对象-播放动画  [回目录](#目录)  
播放动画  
  
#### 输入  
 
```javascript  
{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	bPlay:1, //播放/暂停，1是播放，0是播放
	LoopPlay:1, //循环播放
	PlayRate:1.0, //播放速率
	StartOffset:0.0, //开始偏移
	bRandomStart : false, //随机开始
	bRestoreStatus : false, //恢复状态
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("object.playAnimation",{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	LoopPlay:1, //循环播放
	PlayRate:1.0, //播放速率
	StartOffset:0.0, //开始偏移
	bRandomStart : false, //随机开始
	bRestoreStatus : false, //恢复状态
	bEndPause : false, //末尾处暂停
})  
```  
  
### object.playAnimationSet  
对象-播放动画集  [回目录](#目录)  
播放动画集  
  
#### 输入  
 
```javascript  
{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	Animation: "", //动画名称
	bPlay: "", //播放/暂停，1是播放，0是播放
	LoopPlay:1, //循环播放
	PlayRate:1.0, //播放速率
	StartOffset:0.0, //开始偏移
	bRandomStart : false, //随机开始
	bRestoreStatus : false, //恢复状态
	RestoreLastStatus : "", //恢复上一个动画(ERLS_RESET / ERLS_REVERSE / ERLS_RETAIN)
}  
```  
  
#### 输出  
 
```typescript  
```  
示例数据  
```javascript  
```
  
#### 调用  
 
```typescript
piclient.emit("object.playAnimationSet",{
	target: "", //目标, 可以是Actor子类的蓝图目标(虽然大纲视图不显示)
	Animation: "", //动画名称
	bPlay: "", //是否播放动画
	LoopPlay:1, //循环播放
	PlayRate:1.0, //播放速率
	StartOffset:0.0, //开始偏移
	bRandomStart : false, //随机开始
	bRestoreStatus : false, //恢复状态
	bEndPause : false, //末尾处暂停
	RestoreLastStatus : "", //恢复上一个动画(ERLS_RESET / ERLS_REVERSE / ERLS_RETAIN)
})  
```  
  
### object.playScatterSetAnimation  
对象-播放阵列动画  [回目录](#目录)  
播放阵列对象特定的动画，前提是阵列对象存在，且设置好了动画参数  
  
#### 输入  
 
	```javascript  
	{  
		"objectId": "", // 阵列对象ID
		"animationParamter": "0/0" // 两级阵列播放序列
	}  
	```  
	  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript  
piclient.emit("object.playScatterSetAnimation",{
	"objectId": "", // 阵列对象ID
	"animationParamter": "0/0" // 两级阵列播放序列
})
```  
  
### object.resetScatterSetWithId  
对象-重置阵列对象  [回目录](#目录)  
重置阵列对象到配置的状态  
  
#### 输入  
 
	```javascript  
	{  
		"objectId": "", // 阵列对象ID
	}  
	```  
	  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.resetScatterSetWithId",{
	"objectId": "", // 阵列对象ID
})  
```  
  
### object.setWaterMaterialProperty  
对象-修改水材质属性  [回目录](#目录)  
修改水材质属性  
  
#### 输入  
 
```javascript  
{
	target: "", //对象 
	PropertyFractionIn: "",         //材质透明浑浊度
	PropertyWaterRoughness: "",     //反射强度
	PropertyWaterSpecular: "",      //高光度
	PropertycColor: "",             //水面颜色
	PropertyDirection_Offset: "",   //水流方向
	PropertyFoamOffset: "",         //泡沫流动方向
	PropertyGlobalSpeed: "",        //泡沫流动速率
	PropertySize: "",               //波浪大小
	PropertyWavelength: "",         //波浪强度
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript 
piclient.emit("object.setWaterMaterialProperty",{
	target: "", //对象 
	PropertyFractionIn: "",         //材质透明浑浊度
	PropertyWaterRoughness: "",     //反射强度
	PropertyWaterSpecular: "",      //高光度
	PropertycColor: "",             //水面颜色
	PropertyDirection_Offset: "",   //水流方向
	PropertyFoamOffset: "",         //泡沫流动方向
	PropertyGlobalSpeed: "",        //泡沫流动速率
	PropertySize: "",               //波浪大小
	PropertyWavelength: "",         //波浪强度
}) 
```  
  
### object.blur  
对象-虚化  [回目录](#目录)  
对象虚化  
  
#### 输入  
 
```javascript  
{
	target: "",   //对象Id
	bBlur: "true" //true是开启虚化，false是关闭虚化
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.blur",{
	target: "",   //对象Id
	bBlur: "" //
})  
```  
  
### Object.Opacity  
对象-透明度  [回目录](#目录)  
对象设置透明度  
  
#### 输入  
 
```javascript  
{
	target: "",   //对象Id
	Opacity: "" //
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("Object.Opacity",{
	target: "",   //对象Id
	Opacity: "" //
})  
```  
  
### object.GetInfo  
对象-信息  [回目录](#目录)  
得到对象信息  
  
#### 输入  
 
```javascript  
{
	target: ""   //对象Id
}  
```  
  
#### 输出  
 
```typescript  
{
	targetId:  "",
	DisplayName: "",
	class: "",
	Geo: "",
	Pos:  ""
}
``` 
  
#### 调用  
 
```typescript
piclient.emit("object.GetInfo",{
	targetId:  "",
	DisplayName: "",
	class: "",
	Geo: "",
	Pos:  ""
})  
```  
  
### object.GetScreenInfo  
对象-屏幕信息  [回目录](#目录)  
得到对象屏幕坐标  
  
#### 输入  
 
```javascript  
{
	target: ""  , //对象Id
	selectionIndex: ""
}  
```  
  
#### 输出  
 
```typescript  
{
	targetId:  "",
	projectId: "",
	SrceenObjects: []
}
``` 
  
#### 调用  
 
```typescript  
```  
  
### object.StopScreenInfo  
对象-停止屏幕信息  [回目录](#目录)  
停止推送对象屏幕信息  
  
#### 输入  
 
```javascript  
{
	target: "",   //对象Id
	selectionIndex: "",
    isAll: ""
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript  
```   
  

## Level   
#  
  
[回目录](#目录)  API数量 0004  
### pathfollow.getchecklist  
巡检-获取巡检目标  [回目录](#目录)  
获取该巡检路线的所有巡检目标  
  
#### 输入  
 
```typescript
{
	target: "", //target object name
}
```  
  
#### 输出  
 
```typescript  
{
	checktargetList:[]
}
```  
  
#### 调用  
 
```typescript
piclient.emit("pathfollow.getchecklist",{
	target: "", //target object name
}).then(data => {
	console.log(data)
})  
```  
  
### pathfollow.controll  
巡检-控制  [回目录](#目录)  
控制该巡检路线  
  
#### 输入  
 
```typescript
{
	target: "", //target object name
	type: "" //控制类型 play / pause / stop
}
```  
  
#### 输出  
 
```typescript  
{
}
```  
  
#### 调用  
 
```typescript
piclient.emit("pathfollow.controll",{
	target: "", //target object name
	type: "" //控制类型 play / pause / stop
})  
```  
  
### camera.zoom  
相机-缩放  [回目录](#目录)  
控制相机缩放值  
  
#### 输入  
 
```typescript
{
	value: "", //zoom value
}
```  
  
#### 输出  
 
```typescript  
{
}
```  
  
#### 调用  
 
```typescript
piclient.emit("camera.zoom",{
	value: "", //zoom value
})  
```  
  
### updatelocation.withreference  
更新位置-以参照物为基点  [回目录](#目录)  
以参照物为基点更新场景中物体的位置  
  
#### 输入  
 
```typescript
{
	reference: "", //参照物（坐标基点）
	referencerotation: "", //参照物的旋转量
	target: ""，//需要更新位置的物体
	targetlocation: "", //以参照物为基点要更新的位置
	targetrotation: "", //以参照物的旋转量为基点要更新的旋转量
}
```  
  
#### 输出  
 
```typescript  
{
}
```  
  
#### 调用  
 
```typescript
piclient.emit("updatelocation.withreference",{
	reference: "", //参照物（坐标基点）
	referencerotation: "", //参照物的旋转量
	target: ""，//需要更新位置的物体
	targetlocation: "", //以参照物为基点要更新的位置
	targetrotation: "", //以参照物的旋转量为基点要更新的旋转量
})  
```  
  

## POI   
#  
POI模块，提供点位显示支持  
[回目录](#目录)  API数量 0005  
### poi.createPOI  
POI-创建POI  [回目录](#目录)  
创建POI  
  
#### 输入  
 
```typescript  
{
	geoPos: "121.1234,31.1234,30", //经纬度，经度,纬度[,高程] 支持可选的高程
	parent: "",  //附加的父对象，为空表示独立存在
	relativePos: "100.1234,200,0",  //相对父级的位置  
	text: "", //显示的文本
	iconChar: "\ue030", //图标字符，具体的字符编号参考客户端的图标库表格
	color: "ff0000", //图标颜色
	type: "",   //Poi样式类型
	level: 10 ,//图标级别
	focusOnClick: false, //点击时聚焦到位置
}
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("poi.createPOI",{
	geoPos: "121.1234,31.1234,30", //经纬度，经度,纬度[,高程] 支持可选的高程
	parent: "",  //附加的父对象，为空表示独立存在
	relativePos: "100.1234,200,0",  //相对父级的位置  
	text: "", //显示的文本
	iconChar: "\ue030", //图标字符，具体的字符编号参考客户端的图标库表格
	color: "ff0000", //图标颜色
	type: "",   //Poi样式类型
	level: 10 ,//图标级别
	focusOnClick: false, //点击时聚焦到位置
})  
```  
  
### poi.modifyPOI  
POI-修改POI  [回目录](#目录)  
修改POI  
  
#### 输入  
 
```typescript  
{
	poiid: "",   //POI id
	geoPos: "121.1234,31.1234,30", //经纬度，经度,纬度[,高程] 支持可选的高程
	isgeotype: false,    //坐标类型是geo或Ue坐标
	modelstatus: "", //气泡状态
	visibility: "", //显隐
    labeldata: "",   //文字内容
    labelsize: "",  //文字大小
	labelcColor: "", //文字颜色
    IconWebUrl: "", //网络图片路径
}
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("poi.modifyPOI",{
	poiid: "",   //POI id
	geoPos: "121.1234,31.1234,30", //经纬度，经度,纬度[,高程] 支持可选的高程
	isgeotype: false,    //坐标类型是geo或Ue坐标
	modelstatus: "", //气泡状态
	visibility: "", //显隐
    labeldata: "",   //文字内容
    labelsize: "",  //文字大小
	labelcColor: "", //文字颜色
    IconWebUrl: "", //网络图片路径
})  
```  
  
### poi.loadPOIFromFile  
POI-从文件批量加载POI  [回目录](#目录)  
批量加载POI  
  
#### 输入  
 
```typescript  
{
	append: false, //是否追加模式，false=清空原有
	groupId: "id",  //组ID，为空或者ID不存在则自动创建新的组，存在的话则更新组里的poi
	filePath: "C:/Users/xxx/fileToLoad.json" //文件地址，内容参考poi.loadPOIFromData
} 
```  
  
#### 输出  
 
{
	groupId: "id", //poi组的ID
} 
  
#### 调用  
 
```typescript
piclient.emit("poi.loadPOIFromFile",{
	append: false, //是否追加模式，false=清空原有
	groupId: "id",  //组ID，为空或者ID不存在则自动创建新的组，存在的话则更新组里的poi
	filePath: "C:/Users/xxx/fileToLoad.json" //文件地址，内容参考poi.loadPOIFromData
})  
```  
  
### poi.loadPOIFromData  
POI-从数据批量加载POI  [回目录](#目录)  
批量加载POI  
  
#### 输入  
 
```typescript  
{
	append: false, //是否追加模式，false=清空原有
	groupId: "id",  //组ID，为空或者ID不存在则自动创建新的组，存在的话则更新组里的poi
	data: [
		{ id: "", name: "", iconChar: "\ue030", level: 11, geoPos: "", color: "", type: "", focusOnClick: false }, {}
	]
} 
```  
  
#### 输出  
 
{
	groupId: "id", //poi组的ID
} 
  
#### 调用  
 
```typescript
piclient.emit("poi.loadPOIFromData",{
	append: false, //是否追加模式，false=清空原有
	groupId: "id",  //组ID，为空或者ID不存在则自动创建新的组，存在的话则更新组里的poi
	data: [
		{ id: "", name: "", iconChar: "\ue030", level: 11, geoPos: "", color: "", type: "", focusOnClick: false }, {}
	]
})  
```  
  
### poi.removePOIFromGroup  
POI-从POI组移除POI  [回目录](#目录)  
从POI组移除POI，移除POI组或独立POI使用通用的对象销毁接口  
  
#### 输入  
 
```typescript  
{
	groupId: "id", //poi组的ID
	poiIds: ["",""] //poiId 列表
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("poi.removePOIFromGroup",{
	groupId: "id", //poi组的ID
	poiIds: ["",""] //poiId 列表
})  
```  
  

## Project   
#  
项目相关的接口  
[回目录](#目录)  API数量 0003  
### project.new  
项目-新建项目  [回目录](#目录)  
新建项目  
  
#### 输入  
 
```typescript
{
	projectName: "", //项目名
	geoPos:"", //地理位置
	radius: "", //区域半径，单位米
}
```  
  
#### 输出  
 
```typescript  
{
}
```  
  
#### 调用  
 
```typescript
piclient.emit("project.new",{
	projectName: "", //项目名
	geoPos:"", //地理位置
	radius: "", //区域半径，单位米
})  
```  
  
### project.load  
项目-加载项目  [回目录](#目录)  
加载项目  
  
#### 输入  
 
```typescript
{
	projectId: "", //打开的项目ID
	editorMode: false, //是否默认进入编辑模式
	showUI: true, //是否显示UI
}
```  
  
#### 输出  
 
```typescript  
{
	success: true, 
	msg: "",
}
```  
  
#### 调用  
 
```typescript
piclient.emit("project.load",{
	projectId: "", //打开的项目ID
	editorMode: false, //是否默认进入编辑模式
	showUI: true, //是否显示UI
})  
```  
  
### project.save  
项目-保存项目  [回目录](#目录)  
保存项目  
  
#### 输入  
 
```typescript
{
}
```  
  
#### 输出  
 
```typescript  
{
	success: true, 
	msg: "",
}
```  
  
#### 调用  
 
```typescript
piclient.emit("project.save",{
}).then(data => {
	console.log(data)
})   
```  
  

## UI   
#  
UI模块主要做全局的UI状态的控制  
[回目录](#目录)  API数量 0001  

## Water   
#  
水利模块，提供水利行业支持  
[回目录](#目录)  API数量 0011  
### water.loadMesh  
水利-加载水体网格结构  [回目录](#目录)  
加载水体网格结构，包含地面覆盖层和水系河床网格，可整体加载，也可以分片增量加载  
  
#### 输入  
 
```typescript  
{
	bAppend: true, //追加还是清空重建
	filePath: "C:/Users/xxx/mesh.data" //文件地址，.geojson或.data文件路径
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.loadMesh",{
	bAppend: true, //追加还是清空重建
	filePath: "C:/Users/xxx/mesh.data" //文件地址，.geojson或.data文件路径
})  
```  
  
### water.loadMeshFromData  
水利-从数据加载水体网格结构  [回目录](#目录)  
加载水体网格结构，包含地面覆盖层和水系河床网格，可整体加载，也可以分片增量加载  
  
#### 输入  
 
```typescript  
{
	bAppend: true, //追加还是清空重建
	data: { //格式和water.loadMesh的文件格式一样
		type: "FeatureCollection",
		name: "E:\\网格.geojson",
		features: [        
			{ type: "Feature", properties: { ID: "1", node1: "123", node2: "123", node3: "123" }, geometry: { type: "Polygon", coordinates: [ [ [ 123.123, 31.123 ], [ 123.123, 31.123 ], [ 123.123, 31.123 ], [ 123.123, 31.123 ] ] ] } }
		]
	}
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.loadMeshFromData",{
	bAppend: true, //追加还是清空重建
	data: { //格式和water.loadMesh的文件格式一样
		type: "FeatureCollection",
		name: "E:\\网格.geojson",
		features: [        
			{ type: "Feature", properties: { ID: "1", node1: "123", node2: "123", node3: "123" }, geometry: { type: "Polygon", coordinates: [ [ [ 123.123, 31.123 ], [ 123.123, 31.123 ], [ 123.123, 31.123 ], [ 123.123, 31.123 ] ] ] } }
		]
	}
})  
```  
  
### water.focusOnTile  
水利-聚焦到网格  [回目录](#目录)  
聚焦到指定网格  
  
#### 输入  
 
```typescript  
{
	tileId: 123 //网格id
} 
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.focusOnTile",{
	tileId: 123 //网格id
})  
```  
  
### water.loadStatus  
水利-加载水体状态数据  [回目录](#目录)  
加载水体状态数据，用于更新网格的状态，可整体加载，也可以分片增量加载  
  
#### 输入  
 
```typescript  
{
	filePath: "C:/Users/xxx/status.data" //文件地址，.csv或.data文件路径
} 
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.loadStatus",{
	filePath: "C:/Users/xxx/status.data" //文件地址，.csv或.data文件路径
})  
```  
  
### water.playConfig  
水利-播放状态的控制  [回目录](#目录)  
播放状态的控制，播放速度，播放的时间范围，是否循环等  
  
#### 输入  
 
```typescript  
{
	play: "" //播放状态, play, stop, toggle
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.playConfig",{
	play: "" //播放状态, play, stop, toggle
})  
```  
  
### water.getMeshStat  
水利-水体网格的统计  [回目录](#目录)  
获取水体网格的统计信息，中心点，面积，三角形数量等  
  
#### 输入  
 
```typescript  
{
}  
```  
  
#### 输出  
 
```typescript  
{
	nodeCount: 0, //节点数量
	nodeLoaded: 0, //加载的数量
	areaOrigin: "123.1234,31.1234", //区域面积
	areaExtent: "5000,2000", //区域半径
}
```  
  
#### 调用  
 
```typescript
piclient.emit("water.getMeshStat",{
	nodeCount: 0, //节点数量
	nodeLoaded: 0, //加载的数量
	areaOrigin: "123.1234,31.1234", //区域面积
	areaExtent: "5000,2000", //区域半径
})  
```  
  
### water.getStatusStat  
水利-状态数据的统计  [回目录](#目录)  
获取水体状态数据的统计信息，比如总时长，总帧数等  
  
#### 输入  
 
```typescript  
{
}  
```  
  
#### 输出  
 
```typescript  
{
	nodeCount: 0, //节点数量
	frameCount: 0, //状态帧数
	nodeLoaded: 0, //加载数量
	avgU: 0, //平均U
	avgV: 0, //平均V
	avgH: 0, //平均H
}
```
  
#### 调用  
 
```typescript
piclient.emit("water.getStatusStat",{
	nodeCount: 0, //节点数量
	frameCount: 0, //状态帧数
	nodeLoaded: 0, //加载数量
	avgU: 0, //平均U
	avgV: 0, //平均V
	avgH: 0, //平均H
})  
```  
  
### water.getStatusAtPos  
水利-获取指定位置状态  [回目录](#目录)  
获取指定位置（经纬度）的水体状态  
  
#### 输入  
 
```typescript  
{
	geoPos: "", //地理地址
	nodeId: 123, //节点id，geoPos不为空则忽略此值
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 123,
	x: 0,
	y: 0,
	eta: 0,
	u: 0,
	v: 0,
	h: 0,
	vMag: 0,
	dir: 0
}
```
  
#### 调用  
 
```typescript
piclient.emit("water.getStatusAtPos",{
	geoPos: "", //地理地址
	nodeId: 123, //节点id，geoPos不为空则忽略此值
}).then(data => {
	console.log(data)
})   
```  
  
### water.setStatusAtPos  
水利-更新指定位置状态  [回目录](#目录)  
单独更新指定位置（经纬度）的水体状态  
  
#### 输入  
 
```typescript  
{
	id: 123,
	x: 0,
	y: 0,
	eta: 0,
	u: 0,
	v: 0,
	h: 0,
	vMag: 0,
	dir: 0
}
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.setStatusAtPos",{
	id: 123,
	x: 0,
	y: 0,
	eta: 0,
	u: 0,
	v: 0,
	h: 0,
	vMag: 0,
	dir: 0
})  
```  
  
### water.setStatusConfig  
水利-设置水体状态的配置  [回目录](#目录)  
设置水体状态的配置，比如不同流速，水位的配色，触发警报的阈值等  
  
#### 输入  
 
```typescript  
{
	viewProp: "", //热力图呈现的使用的属性, 为空或者animation表示随机颜色
	rangeValues: "0.5,0.8,100,300",//颜色分段数值范围
	rangeColors: "ff0000,ff1f00,ff2f00,ff3f00", //数值分段的颜色
	opaqueOnZero: false, //数值为0时是否不透明
	altitude: 100, //高程，单位米，网格的高度
	snapGround: false, //是否贴附地形
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.setStatusConfig",{
	viewProp: "", //热力图呈现的使用的属性, 为空或者animation表示随机颜色
	rangeValues: "0.5,0.8,100,300",//颜色分段数值范围
	rangeColors: "ff0000,ff1f00,ff2f00,ff3f00", //数值分段的颜色
	opaqueOnZero: false, //数值为0时是否不透明
	altitude: 100, //高程，单位米，网格的高度
	snapGround: false, //是否贴附地形
})  
```  
  
### water.setDrawCount  
水利-设置绘制数量  [回目录](#目录)  
设置最大同屏绘制数量  
  
#### 输入  
 
```typescript  
{
	drawCount: 123 //最大同屏绘制数量
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("water.setDrawCount",{
	drawCount: 123 //最大同屏绘制数量
})  
```  
  

## Window   
#  
多窗口模块用于在主窗口内部或者独立于主窗口创建多个可以自由移动和改变尺寸的窗口。可以将UI界面，相机，三维模型，网页等内容放在独立的窗口中。窗口模块的很多功能需要C++或者蓝图才能使用，API层面只暴露方便直接使用的版本。  
[回目录](#目录)  API数量 0006  
### window.close  
窗口-关闭  [回目录](#目录)  
关闭指定窗口  
  
#### 输入  
 
```javascript  
{
	windowId: 100, //窗口id
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 100 //窗口id，用于关闭操作
}
```  
  
#### 调用  
 
```typescript
piclient.emit("window.close",{
	id: 100 //窗口id，用于关闭操作
})  
```  
  
### window.move  
窗口-移动位置  [回目录](#目录)  
移动指定窗口的位置  
  
#### 输入  
 
```javascript  
{
	windowId: 100, //窗口id
	position: "400,300", //位置
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 100 //窗口id，用于关闭操作
}
```  
  
#### 调用  
 
```typescript
piclient.emit("window.move",{
	windowId: 100, //窗口id
	position: "400,300", //位置
})  
```  
  
### window.resize  
窗口-更改大小  [回目录](#目录)  
修改指定窗口的大小  
  
#### 输入  
 
```javascript  
{
	windowId: 100, //窗口id
	size: "400,300", //尺寸
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 100 //窗口id，用于关闭操作
}
```  
  
#### 调用  
 
```typescript
piclient.emit("window.resize",{
	windowId: 100, //窗口id
	size: "400,300", //尺寸
})  
```  
  
### window.createByCamera  
窗口-创建相机窗口  [回目录](#目录)  
创建新窗口，并显示指定摄像机的实时画面  
  
#### 输入  
 
```javascript  
{
	title: "", //窗口标题
	standalone: false, //是否独立窗口, false=在主窗口内显示，适合云渲染。true=独立于主窗口，适合多显示器
	position:"0,0", //位置
	size: "400,300", //尺寸
	cameraObject: "SceneCamera_01", //任意带摄像机组件的对象
	cameraIndex: 0 //第几个相机，从0开始
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 100 //窗口id，用于关闭操作
}
```  
  
#### 调用  
 
```typescript
piclient.emit("window.createByCamera",{
	title: "", //窗口标题
	standalone: false, //是否独立窗口, false=在主窗口内显示，适合云渲染。true=独立于主窗口，适合多显示器
	position:"0,0", //位置
	size: "400,300", //尺寸
	cameraObject: "SceneCamera_01", //任意带摄像机组件的对象
	cameraIndex: 0 //第几个相机，从0开始
})  
```  
  
### window.createByWidget  
窗口-创建原生界面窗口  [回目录](#目录)  
创建新窗口，并加载指定UMG原生页面  
  
#### 输入  
 
```javascript  
{
	title: "", //窗口标题
	standalone: false, //是否独立窗口, false=在主窗口内显示，适合云渲染。true=独立于主窗口，适合多显示器
	position:"0,0", //位置
	size: "400,300", //尺寸
	widget: "/Game/UI/TestUI.TestUI_C" //界面地址,注意蓝图路径后面的_C后缀
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 100 //窗口id，用于关闭操作
}
```  
  
#### 调用  
 
```typescript
piclient.emit("window.createByWidget",{
	title: "", //窗口标题
	standalone: false, //是否独立窗口, false=在主窗口内显示，适合云渲染。true=独立于主窗口，适合多显示器
	position:"0,0", //位置
	size: "400,300", //尺寸
	widget: "/Game/UI/TestUI.TestUI_C" //界面地址,注意蓝图路径后面的_C后缀
})  
```  
  
### window.createByWebPage  
窗口-创建网页窗口  [回目录](#目录)  
创建新窗口，并加载指定网页页面  
  
#### 输入  
 
```javascript  
{
	title: "", //窗口标题
	standalone: false, //是否独立窗口, false=在主窗口内显示，适合云渲染。true=独立于主窗口，适合多显示器
	position:"0,0", //位置
	size: "400,300", //尺寸
	url: "https://www.bing.com" //网页地址
}  
```  
  
#### 输出  
 
```typescript  
{
	id: 100 //窗口id，用于关闭操作
}
```  
  
#### 调用  
 
```typescript
piclient.emit("window.createByWebPage",{
	title: "", //窗口标题
	standalone: false, //是否独立窗口, false=在主窗口内显示，适合云渲染。true=独立于主窗口，适合多显示器
	position:"0,0", //位置
	size: "400,300", //尺寸
	url: "https://www.bing.com" //网页地址
})  
```  
  
# 事件详情  


## ModuleBase   
#  
  
[回目录](#目录)  事件数量 0000  

## Camera   
#  
  
[回目录](#目录)  事件数量 0000  

## Cloud   
#  
云渲染相关的接口  
[回目录](#目录)  事件数量 0000  

## Environment   
#  
模式模块主要做环境的管理  
[回目录](#目录)  事件数量 0000  

## General   
#  
通用模块，提供通用功能与服务  
[回目录](#目录)  事件数量 0000  

## GIS   
#  
GIS模块，提供地理消息行业支持  
[回目录](#目录)  事件数量 0000  

## IOT   
#  
IOT模块，提供物联网行业支持  
[回目录](#目录)  事件数量 0000  

## Camera   
#  
  
[回目录](#目录)  事件数量 0000  

## Mode   
#  
模式模块主要做编辑模式的管理  
[回目录](#目录)  事件数量 0000  

## Objects   
#  
  
[回目录](#目录)  事件数量 0009  
### object.onClick  
对象-被点击  [回目录](#目录)  
当对象被鼠标点击时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	componentIndex: 0,//组件序号, -1表示所有组件全部替换
	sectionIndex: 0, //材质位置序号，-1表示所有部位全部替换
	materialId: "", //材质ID
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onClick",{
	target: "", //对象名称
	componentIndex: 0,//组件序号, -1表示所有组件全部替换
	sectionIndex: 0, //材质位置序号，-1表示所有部位全部替换
	materialId: "", //材质ID
})  
```  
  
### object.onDoubleClick  
对象-被双击  [回目录](#目录)  
当对象被鼠标双击时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	componentIndex: 0,//组件序号, -1表示所有组件全部替换
	sectionIndex: 0, //材质位置序号，-1表示所有部位全部替换
	materialId: "", //材质ID
}  
```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onDoubleClick",{
	target: "", //对象名称
	componentIndex: 0,//组件序号, -1表示所有组件全部替换
	sectionIndex: 0, //材质位置序号，-1表示所有部位全部替换
	materialId: "", //材质ID
})  
```    
  
### object.onMoved  
对象-被移动  [回目录](#目录)  
当对象被坐标轴拖动或者通过设置位置移动后触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	geo: "",
}   

```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onMoved",{
	target: "", //对象名称
	geo: "",
})  
```  
  
### object.onScale  
对象-缩放  [回目录](#目录)  
对象缩放大小时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
}   

```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onScale",{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
})  
```  
  
### object.onVisible  
对象-显隐  [回目录](#目录)  
对象显示隐藏状态变化时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
}   

```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onVisible",{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
})  
```  
  
### object.onCreate  
对象-创建  [回目录](#目录)  
对象被创建时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
}   

```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onCreate",{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
})  
```  
  
### object.onDestroy  
对象-销毁  [回目录](#目录)  
对象被销毁时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
}   

```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onDestroy",{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
})  
```  
  
### object.onAttach  
对象-附加  [回目录](#目录)  
对象附加到另外一个对象时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
}   

```  
  
#### 输出  
 
```typescript
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onAttach",{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
})  
```  
  
### object.onDetach  
对象-脱离  [回目录](#目录)  
对象从其他对象上脱离时触发  
  
#### 输入  
 
```javascript  
{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
}   

```  
  
#### 输出  
 
```typescript  
```  
  
#### 调用  
 
```typescript
piclient.emit("object.onDetach",{
	target: "", //对象名称
	params: { objects: ["", "", ""] }
})  
```  
  

## Level   
#  
  
[回目录](#目录)  事件数量 0001  
### pathfollow.onCheck  
巡检-检查对象  [回目录](#目录)  
当巡检到某对象时触发  
  
#### 输入  
 
```javascript  
{
}  
```  
  
#### 输出  
 
```typescript  
{
	pathfollow: "",
	checktarget: ""
}
```

#### 调用  
 
```typescript
piclient.emit("pathfollow.onCheck",{
}).then(data => {
	console.log(data)
})   
``` 
  

## POI   
#  
POI模块，提供点位显示支持  
[回目录](#目录)  事件数量 0000  

## Project   
#  
项目相关的接口  
[回目录](#目录)  事件数量 0000  

## UI   
#  
UI模块主要做全局的UI状态的控制  
[回目录](#目录)  事件数量 0000  

## Water   
#  
水利模块，提供水利行业支持  
[回目录](#目录)  事件数量 0000  

## Window   
#  
多窗口模块用于在主窗口内部或者独立于主窗口创建多个可以自由移动和改变尺寸的窗口。可以将UI界面，相机，三维模型，网页等内容放在独立的窗口中。窗口模块的很多功能需要C++或者蓝图才能使用，API层面只暴露方便直接使用的版本。  
[回目录](#目录)  事件数量 0000  

*-end-*
